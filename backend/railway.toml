[build]
builder = "NIXPACKS"
buildCommand = "npm install --ignore-scripts --production"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10